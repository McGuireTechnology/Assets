Before running project commands, activate the repo-local Mise environment:

```sh
source ./scripts/activate.sh
```

If activation fails because Mise is not installed yet, run:

```sh
./scripts/install.sh
source ./scripts/activate.sh
```
